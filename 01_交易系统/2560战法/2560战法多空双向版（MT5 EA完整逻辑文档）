# 2560战法多空双向版（MT5 EA完整逻辑文档）

本篇文档在原版纯多头“2560战法”的基础上，进行了镜像扩展以支持多空双向交易，并**全面补充了EA程序化开发所需的全部量化定义、资金管理、风控模块与信号确认机制**。本逻辑文档可直接作为MT5 MQL5编程的开发需求书。

---

## 一、 核心指标与输入参数定义

EA开发中，所有核心参数均需开放为外部输入变量（`input`），以便在不同品种（如黄金、原油、股指）和不同时间周期中进行优化测试。

### 1. 核心指标参数
| 参数名称 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| `Inp_MA_Period` | int | 25 | 价格趋势生命线（MA25）周期 |
| `Inp_Vol5_Period` | int | 5 | 短期资金活跃度（VOL5）周期 |
| `Inp_Vol60_Period` | int | 60 | 长期资金基准线（VOL60）周期 |

### 2. 量化阈值参数
| 参数名称 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| `Inp_Vol_Sticky_Ratio` | double | 0.15 | 均量线粘合阈值（如15%，表示两线差值在15%以内） |
| `Inp_Price_Pullback_ATR` | double | 1.0 | 价格回踩阈值（如1.0倍ATR以内视为回踩到位） |
| `Inp_Abnormal_Vol_Ratio` | double | 2.5 | 异常放量阈值（如单根成交量超过VOL60的2.5倍） |

### 3. 资金管理与风控参数
| 参数名称 | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| `Inp_Magic_Number` | int | 2560001 | EA订单魔术数字，用于订单隔离 |
| `Inp_Max_Orders` | int | 3 | 同向最大持仓单数（限制加仓次数） |
| `Inp_Lot_Size` | double | 0.1 | 基础开仓手数 |
| `Inp_Risk_Percent` | double | 2.0 | 固定风险比例（若启用，按余额2%计算手数） |
| `Inp_StopLoss_ATR` | double | 2.0 | 初始止损ATR倍数 |
| `Inp_TakeProfit_ATR` | double | 4.0 | 初始止盈ATR倍数 |
| `Inp_Trailing_Start_ATR`| double | 1.5 | 启动移动止损的盈利ATR倍数 |

---

## 二、 信号确认机制与市场环境判定

### 1. 信号确认机制
**所有交易信号必须在K线收盘时确认（`OnBar` 机制）。**
即仅在当前K线（K0）开盘的第一跳（Tick）时，通过判断上一根已收盘K线（K1）和上上根K线（K2）的状态来触发信号。严禁在K线未走完时实时开仓，以杜绝信号闪烁问题。

### 2. 市场环境判定
通过K1的收盘价与MA25的相对位置决定：
*   **多头环境**：`Close[1] > MA25[1]`。此时EA仅允许执行做多（Buy）指令。
*   **空头环境**：`Close[1] < MA25[1]`。此时EA仅允许执行做空（Sell）指令。

### 3. 多空切换处理逻辑
当市场环境发生切换（如从多头变为空头，即 `Close[1]` 跌破 `MA25[1]`）：
**必须先平掉所有反向持仓（平多单），然后等待新的顺势开仓信号（空头信号）出现再开仓。** 严禁多空双向锁仓对冲。

---

## 三、 多空双向交易信号逻辑（EA核心规则）

### （一） 多头交易逻辑（做多 Buy）

#### 1. 做多开仓信号（放量进场）
*   **价格条件**：K1收盘价突破 `MA25`（即 `Close[2] < MA25[2]` 且 `Close[1] > MA25[1]`），或者价格在 `MA25` 上方回踩后再次收阳。
*   **量能条件**：`VOL5` 向上穿过 `VOL60`（即 `VOL5[2] <= VOL60[2]` 且 `VOL5[1] > VOL60[1]`），形成量能金叉。
*   **执行**：满足上述条件，且当前多头持仓数 = 0，执行 **Buy** 开仓。

#### 2. 做多加仓信号（缩量加仓 / 缩量低吸）
*   **价格条件**：当前处于多头环境，且价格回调至接近 `MA25`（`Abs(Close[1] - MA25[1]) < Inp_Price_Pullback_ATR * ATR[1]`）。
*   **量能条件A（均量线粘合）**：`Abs(VOL5[1] - VOL60[1]) / VOL60[1] < Inp_Vol_Sticky_Ratio`。
*   **量能条件B（单日缩量）**：`VOL5` 依然在 `VOL60` 上方，但 `Volume[1] < VOL60[1]`。
*   **执行**：满足价格条件，且满足量能条件A或B，且当前多头持仓数 < `Inp_Max_Orders`，执行 **Buy** 加仓（手数同基础手数）。

#### 3. 多头平仓信号（放量离场 / 趋势破坏）
*   **量能异常离场**：`VOL5[1] < VOL60[1]`，但单根成交量异常放大（`Volume[1] > Inp_Abnormal_Vol_Ratio * VOL60[1]`），视为诱多，平掉所有多单。
*   **趋势破坏离场**：`Close[1] < MA25[1]`，多头趋势结束，平掉所有多单。

---

### （二） 空头交易逻辑（做空 Sell）

#### 1. 做空开仓信号（放量进场）
*   **价格条件**：K1收盘价跌破 `MA25`（即 `Close[2] > MA25[2]` 且 `Close[1] < MA25[1]`），或者价格在 `MA25` 下方反弹后再次收阴。
*   **量能条件**：`VOL5` 向上穿过 `VOL60`（即 `VOL5[2] <= VOL60[2]` 且 `VOL5[1] > VOL60[1]`），形成量能金叉。
*   **执行**：满足上述条件，且当前空头持仓数 = 0，执行 **Sell** 开仓。

#### 2. 做空加仓信号（缩量加仓 / 缩量高空）
*   **价格条件**：当前处于空头环境，且价格反弹至接近 `MA25`（`Abs(Close[1] - MA25[1]) < Inp_Price_Pullback_ATR * ATR[1]`）。
*   **量能条件A（均量线粘合）**：`Abs(VOL5[1] - VOL60[1]) / VOL60[1] < Inp_Vol_Sticky_Ratio`。
*   **量能条件B（单日缩量）**：`VOL5` 依然在 `VOL60` 上方，但 `Volume[1] < VOL60[1]`。
*   **执行**：满足价格条件，且满足量能条件A或B，且当前空头持仓数 < `Inp_Max_Orders`，执行 **Sell** 加仓（手数同基础手数）。

#### 3. 空头平仓信号（放量离场 / 趋势破坏）
*   **量能异常离场**：`VOL5[1] < VOL60[1]`，但单根成交量异常放大（`Volume[1] > Inp_Abnormal_Vol_Ratio * VOL60[1]`），视为诱空，平掉所有空单。
*   **趋势破坏离场**：`Close[1] > MA25[1]`，空头趋势结束，平掉所有空单。

---

## 四、 止损止盈与资金管理模块

### 1. 初始止损与止盈（基于ATR动态计算）
*   **做多（Buy）**：
    *   止损价（SL） = 开仓价 - `Inp_StopLoss_ATR` * ATR[1]
    *   止盈价（TP） = 开仓价 + `Inp_TakeProfit_ATR` * ATR[1]
*   **做空（Sell）**：
    *   止损价（SL） = 开仓价 + `Inp_StopLoss_ATR` * ATR[1]
    *   止盈价（TP） = 开仓价 - `Inp_TakeProfit_ATR` * ATR[1]

### 2. 移动止损（Trailing Stop）
当订单浮动盈利达到 `Inp_Trailing_Start_ATR * ATR` 时，启动移动止损。将止损位移动至成本价（保本），随后价格每向有利方向移动一定点数，止损位同向跟进，以锁定利润。

### 3. 仓位计算逻辑（固定风险比例）
若启用固定风险比例（`Inp_Risk_Percent > 0`），则每笔订单手数计算公式为：
`开仓手数 = (账户余额 * Inp_Risk_Percent) / (止损点数 * 每点价值)`
*注意：计算结果需根据平台的最小开仓手数（MinLot）和手数步长（LotStep）进行向下取整修正。若未启用，则默认使用 `Inp_Lot_Size` 固定手数。*

---

## 五、 交易时间过滤模块

为规避低流动性和重大新闻风险，建议EA内置交易时间过滤功能（可通过外部参数开关）：
*   **允许交易时间段**：例如 08:00 - 22:00（平台服务器时间）。在此时段外，EA不触发任何新开仓信号，但允许平仓和移动止损继续工作。
*   **周五强制平仓**：为规避周末跳空风险，若开启此功能，EA将在周五服务器时间 22:30 自动平掉所有持仓。
