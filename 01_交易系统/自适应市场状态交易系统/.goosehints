# 全局工作记录规则（所有目录通用）

## 每次会话开始时，自动执行：
1. 检查当前工作目录下是否有 `工作记录.md` 文件
2. **如果有** → 先读取 `工作记录.md`，了解之前的工作进展，后续每次操作自动追加更新
3. **如果没有** → 自动在当前目录创建一个全新的 `工作记录.md`，写入会话标题和初始信息，后续每次操作自动追加更新

## 工作记录更新规则：
- 每次操作完成后，自动追加最新内容到 `工作记录.md`
- 新会话用 `## YYYY-MM-DD 会话` 作为标题
- 记录内容：做了什么、结果如何、关键发现、下一步待办

## 项目背景
这是一个 MQL5 自适应市场状态交易系统，包含趋势策略、震荡策略、状态机、风险管理等模块。
