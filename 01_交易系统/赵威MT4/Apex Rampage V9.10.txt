//+------------------------------------------------------------------+
//|                                  Apex_Rampage_V9.10_Silencer.mq5 |
//|                     终极狂暴 V9.10-实盘消音版：保留 12k 基因，加入风控隐身 |
//+------------------------------------------------------------------+
#property copyright "Asymmetric System - Apex V9.10 Silencer"
#property version   "9.10"
#property strict

#include <Trade\Trade.mqh>
CTrade tradeA, tradeB, tradeP;

//--- 资金与核心参数 ---
input string   _Config_         = "--- 资金与核心参数 ---";
input double   InpRiskPercent   = 5.0;       // 单次总风险 5% (非对称赌注灵魂)
input int      InpAtrPeriod     = 14;
input double   InpSlMultiplier  = 0.8;       // 极窄初始止损 (保持极小以放大仓位)
input int      InpBreakPeriod   = 10;        // 敏锐突破周期

//--- 双子星逻辑 ---
input string   _Twin_           = "--- 双子星逻辑 ---";
input double   InpTpMultiplierA = 1.5;       // 订单A快速止盈 (血包单)
input double   InpBeMultiplierB = 0.8;       // 订单B平保触发
input double   InpTrailMultiplierB= 2.5;     // 抗洗盘追踪 (必须>=2.5才能扛住深V)

//--- 原始狂暴引擎 ---
input string   _Pyramid_        = "--- 原始狂暴引擎 ---";
input bool     InpEnablePyramid = true;      // 开启加仓
input double   InpPyramidGap    = 1.5;       // 加仓间距 (原始1.5 ATR)
input int      InpMaxAddons     = 5;         // 最大加仓 5 次

//--- 实盘装甲 (V9.10 新增) ---
input string   _Armor_          = "--- 实盘风控装甲 ---";
input int      InpCooldownMs    = 200;       // 操作冷却时间(毫秒)，防止被判DDOS

//--- 全局变量
int handle_atr;
double atr_buffer[];
double base_lot_size = 0;
ulong last_action_time = 0; // 记录最后一次操作的时间戳 (毫秒)

int OnInit() {
   handle_atr = iATR(_Symbol, _Period, InpAtrPeriod);
   ArraySetAsSeries(atr_buffer, true);
   tradeA.SetExpertMagicNumber(1001);
   tradeB.SetExpertMagicNumber(1002);
   tradeP.SetExpertMagicNumber(1003);
   
   // 检查是否有系统权限获取毫秒级时间
   if(GetTickCount64() == 0) Print("警告：系统可能不支持高精度计时。");
   
   return(INIT_SUCCEEDED);
}

void OnTick() {
   // 没有任何休市或点差拦截，全速裸奔！(保留原始爆发力)
   
   // --- V9.10 核心隐形消音器：毫秒级频率限制 ---
   ulong current_time = GetTickCount64();
   if(current_time - last_action_time < (ulong)InpCooldownMs) {
      return; // 仍在冷却中，跳过本次 Tick
   }

   if(CopyBuffer(handle_atr, 0, 0, 2, atr_buffer) <= 0) return;
   
   double current_atr = atr_buffer[0];
   double ask = SymbolInfoDouble(_Symbol, SYMBOL_ASK);
   double bid = SymbolInfoDouble(_Symbol, SYMBOL_BID);
   
   double high_array[], low_array[];
   ArraySetAsSeries(high_array, true);
   ArraySetAsSeries(low_array, true);
   CopyHigh(_Symbol, _Period, 1, InpBreakPeriod, high_array);
   CopyLow(_Symbol, _Period, 1, InpBreakPeriod, low_array);
   double highest_high = high_array[ArrayMaximum(high_array)];
   double lowest_low = low_array[ArrayMinimum(low_array)];
   
   int my_positions = 0;
   for(int i = PositionsTotal() - 1; i >= 0; i--) {
      if(PositionGetSymbol(i) == _Symbol) my_positions++;
   }

   // 突破入场 (完全还原 V3 第一版逻辑)
   if(my_positions == 0) {
      double sl_distance = current_atr * InpSlMultiplier;
      double risk_money_per_trade = AccountInfoDouble(ACCOUNT_BALANCE) * (InpRiskPercent / 100.0) / 2.0;
      double tick_val = SymbolInfoDouble(_Symbol, SYMBOL_TRADE_TICK_VALUE);
      double tick_sz = SymbolInfoDouble(_Symbol, SYMBOL_TRADE_TICK_SIZE);
      
      base_lot_size = NormalizeDouble(risk_money_per_trade / (sl_distance / tick_sz * tick_val), 2);
      base_lot_size = MathMax(base_lot_size, SymbolInfoDouble(_Symbol, SYMBOL_VOLUME_MIN));
      
      bool order_placed = false; // 用于标记是否触发了交易动作
      
      if(ask > highest_high) {
         double sl = bid - sl_distance;
         double tp_A = ask + (current_atr * InpTpMultiplierA);
         
         // 修正点差过大导致的开仓即止损问题 (微调止损以适应实盘)
         sl = NormalizeDouble(MathMin(sl, bid - tick_sz * 10), _Digits); 
         
         if(tradeA.Buy(base_lot_size, _Symbol, ask, sl, tp_A, "Twin A: Cover")) order_placed = true;
         if(tradeB.Buy(base_lot_size, _Symbol, ask, sl, 0, "Twin B: Base")) order_placed = true;
         
      } else if(bid < lowest_low) {
         double sl = ask + sl_distance;
         double tp_A = bid - (current_atr * InpTpMultiplierA);
         
         // 修正点差过大导致的开仓即止损问题
         sl = NormalizeDouble(MathMax(sl, ask + tick_sz * 10), _Digits);
         
         if(tradeA.Sell(base_lot_size, _Symbol, bid, sl, tp_A, "Twin A: Cover")) order_placed = true;
         if(tradeB.Sell(base_lot_size, _Symbol, bid, sl, 0, "Twin B: Base")) order_placed = true;
      }
      
      if(order_placed) last_action_time = GetTickCount64(); // 更新操作时间
      
   } else {
      ManageApexSystemOrigin(current_atr, ask, bid);
   }
}

void ManageApexSystemOrigin(double atr, double ask, double bid) {
   int runner_count = 0;
   int addon_count = 0;
   double last_entry_buy = 0;
   double last_entry_sell = 999999;
   long current_trend_type = -1;
   
   // 第一次扫描：统计状态 (100% 还原 V3 寻找边界的逻辑)
   for(int i = PositionsTotal() - 1; i >= 0; i--) {
      ulong ticket = PositionGetTicket(i);
      if(PositionGetString(POSITION_SYMBOL) != _Symbol) continue;
      long magic = PositionGetInteger(POSITION_MAGIC);
      if(magic != 1002 && magic != 1003) continue;

      long type = PositionGetInteger(POSITION_TYPE);
      current_trend_type = type;
      double open_price = PositionGetDouble(POSITION_PRICE_OPEN);
      
      if(magic == 1002) runner_count++;
      if(magic == 1003) addon_count++;
      
      if(type == POSITION_TYPE_BUY && open_price > last_entry_buy) last_entry_buy = open_price;
      if(type == POSITION_TYPE_SELL && open_price < last_entry_sell) last_entry_sell = open_price;
   }

   // ！！！V3 独有的神级断路器：底仓没了直接不管加仓单 (孤儿单装死机制) ！！！
   if(runner_count == 0) return;

   double trail_distance = atr * InpTrailMultiplierB;
   double pyramid_distance = atr * InpPyramidGap;
   double point = SymbolInfoDouble(_Symbol, SYMBOL_POINT);
   
   bool action_taken = false; // 用于标记是否触发了修改或加仓动作

   // 第二次扫描：遍历执行保护与加仓 (重现嵌套 BUG 链式反应)
   for(int i = PositionsTotal() - 1; i >= 0; i--) {
      ulong ticket = PositionGetTicket(i);
      if(PositionGetString(POSITION_SYMBOL) != _Symbol) continue;
      long magic = PositionGetInteger(POSITION_MAGIC);
      if(magic != 1002 && magic != 1003) continue;

      double open_price = PositionGetDouble(POSITION_PRICE_OPEN);
      double current_sl = PositionGetDouble(POSITION_SL);
      double current_price = PositionGetDouble(POSITION_PRICE_CURRENT);
      long type = PositionGetInteger(POSITION_TYPE);
      
      if(type == POSITION_TYPE_BUY) {
         // 1. 光速推平保
         if(magic == 1002 && current_price - open_price > atr * InpBeMultiplierB && current_sl < open_price) {
            if(tradeB.PositionModify(ticket, open_price + 10 * point, 0)) action_taken = true;
         }

         // 2. 统一追踪止损 (增加一个微小的 buffer 防止 Invalid stops)
         if(current_sl >= open_price || magic == 1003) {
            double new_sl = NormalizeDouble(current_price - trail_distance, _Digits);
            if(new_sl > current_sl + 10 * point) { // Buffer: 至少提高10点才修改
               if(tradeB.PositionModify(ticket, new_sl, 0)) action_taken = true;
            }
         }

         // 3. 狂暴加仓 (写在循环内，重现毫秒级同 Tick 爆发)
         if(InpEnablePyramid && addon_count < InpMaxAddons && current_sl >= open_price) {
            if(ask > last_entry_buy + pyramid_distance) {
               double add_sl = NormalizeDouble(ask - trail_distance, _Digits);
               if(tradeP.Buy(base_lot_size, _Symbol, ask, add_sl, 0, "Pyramid Add Long")) {
                  last_entry_buy = ask; 
                  action_taken = true;
                  addon_count++; // 即时更新计数，稍微约束一下过度疯狂
               }
            }
         }
      }
      else if(type == POSITION_TYPE_SELL) {
         // 1. 推平保
         if(magic == 1002 && open_price - current_price > atr * InpBeMultiplierB && (current_sl > open_price || current_sl == 0)) {
            if(tradeB.PositionModify(ticket, open_price - 10 * point, 0)) action_taken = true;
         }

         // 2. 追踪止损 (增加 buffer)
         if((current_sl <= open_price && current_sl != 0) || magic == 1003) {
            double new_sl = NormalizeDouble(current_price + trail_distance, _Digits);
            if(new_sl < current_sl - 10 * point || current_sl == 0) {
               if(tradeB.PositionModify(ticket, new_sl, 0)) action_taken = true;
            }
         }

         // 3. 狂暴加仓
         if(InpEnablePyramid && addon_count < InpMaxAddons && current_sl <= open_price && current_sl != 0) {
            if(bid < last_entry_sell - pyramid_distance) {
               double add_sl = NormalizeDouble(bid + trail_distance, _Digits);
               if(tradeP.Sell(base_lot_size, _Symbol, bid, add_sl, 0, "Pyramid Add Short")) {
                  last_entry_sell = bid; 
                  action_taken = true;
                  addon_count++; // 即时更新计数
               }
            }
         }
      }
   }
   
   if(action_taken) last_action_time = GetTickCount64(); // 如果本轮有任何动作，重置冷却倒计时
}