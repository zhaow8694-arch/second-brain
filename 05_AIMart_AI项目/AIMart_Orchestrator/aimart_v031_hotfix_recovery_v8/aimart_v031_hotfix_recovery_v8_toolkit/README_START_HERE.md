# AIMart v0.3.1 Hotfix + Recovery V8

Use this toolkit only for the v0.3.1 recovery state.

It fixes the source ZIP entry check by normalizing ZIP paths (`\` -> `/`) and uses host PowerShell/cmd for pnpm, Next.js, release packaging, sample ZIP verification, SHA256, manifest, dogfood evidence, commit, and tag.

Run:

```powershell
& "E:\AIMart_Orchestrator\aimart_v031_hotfix_recovery_v8\aimart_v031_hotfix_recovery_v8_toolkit\START_V0.3.1_HOTFIX_AND_RECOVERY_V8.cmd"
```

Do not run V1-V7 after this.
